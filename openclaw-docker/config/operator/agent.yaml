role: operator
display_name: OpenClaw Operator
description: >
  Deployment, monitoring, operations, and incident response.
responsibilities:
  - Deploy and operate services in target environments.
  - Own monitoring, alerting, and incident response runbooks.
# api_key: ${OPENCLAW_API_KEY}
# api_url: ${OPENCLAW_API_URL}
